# 微信助手项目总体框架

## 项目目标
构建稳定的 Linux 微信自动化平台，采用 **C++ RPA 核心 + Python API 编排 + 前端可视化调试** 的分层架构。

## 总体框架（仅维护状态，不扩增框架条目）

### 1) 后端服务（Backend Service）
- 状态：✅ 已完成（持续维护）
- 职责：REST API、流程编排、状态管理、兼容路由。
- 核心路径：`backend/main.py`、`backend/api/v1/*`。

### 2) RPA 自动化模块（RPA Module）
- 状态：✅ 已完成（持续维护）
- 职责：微信窗口控制、区域扫描、AT-SPI 控件交互、截图/OCR协同。
- 核心组件：`WeChatManager`、`ATSPIEngine`、`HumanizationEngine`、`ImageProcessor`、`OCREngine`、`WindowManager`。

### 3) AI 模块（AI Module）
- 状态：✅ 已完成（持续维护）
- 职责：模型路由、消息理解与生成。

### 4) 数据存储（Data Storage）
- 状态：✅ 已完成（持续维护）
- 职责：配置、会话、消息等持久化存储。

### 5) 前端界面（Frontend Interface）
- 状态：✅ 已完成（持续维护）
- 职责：配置、扫描、构建、验证与联调可视化。

## 大框架完成状态
- ✅ 后端服务框架
- ✅ C++ RPA 核心框架
- ✅ API 聚合与兼容调用框架
- ✅ 前端调试与配置框架
- ✅ 数据持久化框架
- ✅ 扫描任务可观测框架（进度/中断/结果回传）
- ✅ 标注构建确认闭环（构建后截图确认）

## 大框架在途项（不新增条目，仅状态更新）
- 🔄 稳定性回归与异常治理（持续）
- 🔄 跨环境适配与迁移验证（持续）

## 框架策略（保持不变）
1. AT-SPI 优先。
2. 多层降级（AT-SPI -> 几何/坐标 -> OCR辅助）。
3. 拟人化行为保护（随机偏移、随机延迟、输入节奏）。
4. 可观测优先（策略轨迹、扫描进度、区域调试摘要）。

## 更新规则
- 本文档只在“大框架完成”或“大框架级变动”时更新。
- 非框架级接口修复、参数调整、局部UI改动不在此新增条目。
